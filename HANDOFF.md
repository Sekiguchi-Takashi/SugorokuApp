# SugorokuApp（どうぶつすごろく）HANDOFF

## 概要
どうぶつの森風の世界観のすごろくアプリ。プレイヤーはキャラ（しばいぬ・うさぎ・いのしし・トラ）を選び、ルーレット(1〜6)で30マスの盤面を進んでゴールを目指す。

- 現在バージョン: **v5.5-A（B面モジュールを追加：1リポジトリ2アプリ）**
- パッケージ: `com.appathy.sugoroku` / minSdk 26 / targetSdk 34

## ロードマップ
| フェーズ | 内容 | 状態 |
|---|---|---|
| 1 | ルーレット＋すごろく移動＋ゴール判定 | ✅ v1.0 |
| 2 | 停止マスでイベント発生 | ほぼ完了（v2.2: 写真付きイベント10種＋すすむ/もどる7マス。残り: 1回休み・選択イベント・ゴール時スコア） |
| 3 | マルチプレイ | ✅ 完了（最大4キャラ／人間は最大2人のパス&プレイ＋CPU） |
| 4 | データ駆動化 | ✅ 完了（v4.0b: 本線3ステージ＋洞窟をすべてJSON化） |
| 5 | イベントエディタ・図鑑 | ✅ 完了（v4.1） |
| 6 | 職業・資産・スキル | ✅ 完了（v4.2） |
| 7 | モード制・学校編 | ✅ **完成**（小学校＋中学校の2ステージで完結。高校・大学は作らない方針に変更） |

## ビルド規約（変更禁止）
- AGP **8.5.2** / Kotlin **1.9.24** / Gradle **8.9**（CIの setup-gradle で固定、wrapperなし）
- 外部依存ゼロ・XMLレイアウト不使用（プログラマティックUIのみ。AndroidManifest.xmlは除く）
- `debug.keystore` はリポジトリにコミット済み（storepass/keypass: `android`、alias: `androiddebugkey`）
- 納品は差分ZIP、ファイル名はバージョン番号付きで毎回変える

## ⚠️ 重要な落とし穴（ビルドを壊した実例）
- **関数を書き換えるとき、隣接する別の関数を巻き込んで消さない。** v5.4-A で `scoreOf()` をモード別に書き換えた際、`s[start:end]` の範囲指定を誤って直後にあった `meetsCond()` ごと削除し、呼び出しだけ2箇所残って `Unresolved reference` でビルドが落ちた
  - 対策として **`tools/validate.py` に「呼び出しはあるのに定義が無い関数」の検出を追加**した（文字列リテラル・コメント・ラムダ引数・分割宣言を除外。わざと関数を消して検出できることをテスト済み）
  - **Pythonで大きなブロックを置換するときは、置換前後の関数一覧を突き合わせること**
- **文字列テンプレートの直後に日本語を書かない。** Kotlin では日本語も識別子文字なので `"$tag満腹"` は変数 `tag満腹` と解釈され `Unresolved reference` でコンパイルが落ちる（v3.3で実際に発生）。**`${tag}満腹` のように必ず波かっこで囲む**
  - 納品前チェック用の正規表現: `\$([A-Za-z_][A-Za-z0-9_]*)([\u3040-\u30FF\u4E00-\u9FFF])` にマッチする箇所がゼロであること

- **`git init` は必ず `~/SugorokuApp` 内で実行すること。** ホームディレクトリで実行するとトークン等がステージされ Push Protection (GH013) で弾かれる事故が過去に発生済み。

## 構成
```
SugorokuApp/
├── .github/workflows/build.yml   # gradle assembleDebug → APK artifact
├── build.gradle.kts / settings.gradle.kts
├── debug.keystore
└── app/
    ├── build.gradle.kts
    └── src/main/
        ├── AndroidManifest.xml
        ├── kotlin/com/appathy/sugoroku/MainActivity.kt  # 全ロジック1ファイル
        └── res/drawable/
            ├── chara_shiba.png / chara_usagi.png / chara_inoshishi.png / chara_tora.png (512px)
            └── ic_launcher.png (192px, しばいぬ流用)
```

## コード設計（MainActivity.kt）
- 画面遷移は `setContentView` 切替: `showTitle()` → `showCharaSelect()` → `showCountSelect()`（人数1〜4） → `showGame()`
- `Player(chara, isHuman, position)` のリストで管理。`turn` で手番を回す。人間は先頭固定、CPUは残りキャラから順に割当
- CPU手番: `startTurn()` が1秒後に `rouletteView.autoSpin()` を呼ぶ（タップロック中でも回る）
- ルーレット結果: 停止後 `showResult()` が中央に正立・特大の数字をポップ表示（0.35秒拡大アニメ→0.7秒表示→移動開始）。盤の回転と独立して描画するため常に読みやすい向き
- 複数駒が同一マスのときは横にオフセット描画。手番の駒は大きく最前面
- `Board` object: `COLS=5, ROWS=6, CELL_COUNT=30, GOAL_INDEX=29`。盤面は下段左スタートの蛇行（サーペンタイン）配置
- `BoardView`: Canvas描画（マス円・経路線・駒ビットマップ）。`position` 更新→`invalidate()`
- `RouletteView`: 6色セグメント。結果を先に抽選し、その角度に減速停止（DecelerateInterpolator 2.6秒）。`locked` はタップのみ禁止（`autoSpin()` は通る）
- 移動: `movePiece()` が Handler 300ms間隔で1マスずつ進める。ゴール超過は現状「ゴールで停止（clamp）」仕様。誰かがゴールした時点で勝敗確定

## v5.4-A: 学校編 完成

**方針変更**: 高校・大学は作らず、**小学校＋中学校の2ステージで完結**。キャラはどうぶつ8体。

### モード別のUI整理
学校編には しごと・おみせ・けっこん・出産・ワープ が無いので、関連表示を出し分けた。
| | つうじょう版 | 学校編 |
|---|---|---|
| ステータスバー | 💰おかね＋しごとアイコン | 🌸ごうかく／💗こいびと |
| ステータス画面 | おかね・しごと・スキル | ごうかく・こいびと |
| ステージクリア | きゅうりょうの内訳 | 出さない |
| 最終結果 | 🏆ぜんステージ クリア | 🎓そつぎょう おめでとう |
| スコア | ステータス+おかね/10+きゅうりょう/10+スキル×10+かぞく | **ステータス+ごうかく50+こいびと30** |
| 凡例 | 🩵しごと 🟡おみせ 🟠ワープ 🩷けっこん | 🔴じゅけん 🩷こくはく |
| 満腹スキルの絵文字 | 🍖 | 📖 |

### ⚠️ 重大な発見: ゴール前のマスは踏まれない
**28マス目に置いた受験マスを踏める確率は28%しかなかった**（出目で飛び越してゴールしてしまう）。同じ理由でこくはく（16マス目）も21%しか発生していなかった。

対策:
1. **`pendingChallenge()`** — ゴール到達時に未挑戦の受験があれば、**マスを踏んでいなくても必ず受験させる**（`onLanded` のゴール判定内で処理）
2. **こくはくを2箇所に**（中学校 16 と 21）。こちらは救済しない代わりに機会を増やした
3. **`examTries` と `EXAM_MAX_TRIES = 2`** — 救済を入れると失敗しても何度でも粘れて**全員合格(100%)になってしまった**ため、挑戦回数に上限を設けた。残り回数はダイアログに表示する

**教訓: すごろくで「必ず通したいマス」はゴール直前に置いてはいけない。** ゴール判定側で拾うか、複数箇所に置くこと。

### 最終バランス（5000試行）
| 項目 | 結果 |
|---|---|
| 高校受験ごうかく | **73%** |
| こいびとが できた | **36%** |
| りょうほう達成 | 30% |
| 最終スコア | 中央251（30〜456） |

`shared.exam.baseRate = 0` にしたので、**勉強の値がそのまま合格率(%)**になる分かりやすい設計。こくはくは `baseRate 35 + モテモテ`。

## 🔀 A面 / B面 の構成（v5.5〜）

**同一リポジトリ内の2モジュール。完全独立（コード複製）方式。**

| | A面 `:app` | B面 `:bside` |
|---|---|---|
| パッケージ | `com.appathy.sugoroku` | `com.appathy.sugorokub` |
| applicationId | `com.appathy.sugoroku` | `com.appathy.sugoroku.bside` |
| アプリ名 | どうぶつすごろく | がっこうすごろく |
| 内容 | つうじょう版＋学校編 | **学校編のみ** |
| キャラ | どうぶつ8体 | **人間12人** |
| 画像 | 117枚 | 51枚（必要なものだけ） |
| ビルド | `gradle :app:assembleDebug` | `gradle :bside:assembleDebug` |

### なぜ完全独立にしたか
Kotlinを共有する方式（`srcDirs` でA面を参照）も検討したが、**A面を壊すとB面のビルドも落ちる**ため「干渉しない」という要件に合わなかった。コードを複製したので、**A面を何をしてもB面は絶対に変わらない**。

### ⚠️ 二重管理の注意
`MainActivity.kt` などが2セット存在する。**A面のバグ修正はB面へ手作業で反映する必要がある**。
反映の手順:
```
cd ~/SugorokuApp
sed 's/^package com\.appathy\.sugoroku$/package com.appathy.sugorokub/' \
    app/src/main/kotlin/com/appathy/sugoroku/XXX.kt \
    > bside/src/main/kotlin/com/appathy/sugorokub/XXX.kt
```
ただし **B面はモード選択画面を削除している**ので、`MainActivity.kt` を丸ごと上書きすると復活してしまう。B面固有の差分は以下の3点だけなので、上書き後に再適用すること。
1. `GameMode` は `SCHOOL` のみ（`NORMAL` を削除）
2. `mode` の初期値が `GameMode.SCHOOL`
3. `showModeMenu()` を削除し、タイトルの「はじめる」→ `showCharaSelect()` 直行

`mode == GameMode.SCHOOL` の分岐は常に真になるが、**A面との差分を増やさないためあえて残している**。

### B面のassets
- `stages.json` — A面の `school_stages.json` を持ち込み、`charaSet` を `human` に
- `charas.json` — 人間12人（partner/child なし。本人画像にフォールバックする）
- `jobs.json` / `cave.json` — 使わないが、読み込み警告を出さないため最小構成で配置

### 検証
`python3 tools/validate.py`（A面）/ `python3 tools/validate.py bside`（B面）で切り替わる。**両方とも通してから push すること。**

## ⚠️ A面 / B面の分岐（v5.3以降）

**同じリポジトリ名で2系統を並行開発する方針**。このファイルは **A面** のもの。

| | A面（このファイル） | B面 |
|---|---|---|
| キャラ | **どうぶつ8体のみ** | 人間12人 |
| モード | つうじょう版＋学校版（どうぶつが通学する） | **学校編のみ** |
| charas.json | `animal` セットだけ | `human` セットだけ |

### A面で外したもの（B面へ移設）
- `chara_kid01〜12.png`（12枚）を drawable から削除
- `charas.json` から `human` セットを削除
- `school_stages.json` の `charaSet` を `human` → **`animal`** に変更

退避先: **`SugorokuApp_Bside_assets.zip`**（`drawable/chara_kid01〜12.png` と `charas_human.json`）。B面を作るときはこれを展開して `charas.json` に取り込む。

### なぜ分離が容易だったか
v5.2 でキャラ定義を `charas.json` に外部化してあったため、**Kotlinのコードは1行も変えずに** JSONと画像の出し入れだけで切り替えられた。B面も同じ仕組みでキャラセットを差し替えるだけでよい。

### 学校版の文面について
「くつばこに てがみが はいってた」「あたらしい せいふくを きた」など、**主語がどうぶつでも自然に読める**書き方になっているので、A面のまま流用できる。B面で人間向けに変える必要も特にない。

## v5.2: キャラクターのJSON外部化と人間12人

### assets/charas.json（新規）
キャラ定義を Kotlin のハードコードから外部化した。`MainActivity.charas` は `var charas: List<Chara>` になり、モード切替時に読み直される。
```json
{ "schemaVersion": 1,
  "sets": {
    "animal": { "label": "どうぶつ", "charas": [
      {"name":"しばいぬ","img":"chara_shiba","partner":"chara_shiba_f","child":"child_shiba"} ]},
    "human":  { "label": "にんげん", "charas": [
      {"name":"あかり","img":"chara_kid01"} ]}
  } }
```
- **`partner` / `child` は省略可**。省略すると本人画像にフォールバックするので、**学校版のように1キャラ1枚でも表示が欠けない**
- 各ステージJSONに `"charaSet": "animal" | "human"` を書いてセットを指定する
- **これが「Cの受け皿」**: 中学生・高校生・大学生の画像が揃ったら、charas.json にセットを足して各ステージJSONの `charaSet` を差し替えるだけ。**コード変更は不要**

### 人間キャラ12人（chara_kid01〜12）
仮の名前: あかり／みなみ／そうた／ひまり／りく／しおり／はやと／ゆい／けんと／たける／あおい／さくら。**charas.json の `name` を書き換えれば変更できる**。

### 画像の切り抜き（ここが一番苦戦した）
背景がマゼンタのグラデーションで、四隅の色も揃っていなかった。試した順序と結果:
1. **固定しきい値** → 背景のムラで抜けきらない
2. **縁の色を基準にしたRGB距離＋フラッドフィル** → 髪のすき間に閉じ込められたピンクが残る
3. **RGB距離で全域除去** → **明るい茶髪が消えた**。ash-brown の髪（175,148,127）とマゼンタ背景（183,82,128）のRGB距離が66しかなく、しきい値78で巻き込まれていた
4. **✅ 色相（HSV）で判定** → 背景は色相320度前後、茶髪や肌は20〜40度なので確実に分離できる。`|色相差| < 25 かつ 彩度 > 背景彩度*0.45` を背景とみなし、①縁から連結する領域 ②縁とつながらない小さな塊（髪のすき間のピンク）の両方を除去

**教訓: 有彩色の背景を抜くときはRGB距離ではなく色相で判定する。** 明度の近い髪色を巻き込む事故が起きる。加えて、縮小前に透明部のRGBを周囲の色で埋めないと、縮小時に背景色が輪郭へ滲む。

### キャラ数の違いへの対応
`others.getOrNull(i)`、`maxTotal = (humans + others.size).coerceAtMost(4)` を入れ、**キャラが4体未満のセットでも人数選択が破綻しない**ようにした。charas.json が読めない場合はモード選択でToastを出して先へ進ませない。

### validate.py の追加検査
charas.json のセットごとの名前重複・画像の実在、**結婚/出産マスがあるモードのキャラに partner/child が揃っているか**（つうじょう版=必要 / 学校版=不要）を検査する。

## v5.1: ちゅうがっこうステージと「こくはく」

### 受験を「ちょうせん」に一般化（リファクタ）
v5.0 の `showExam()` は受験専用だったが、こくはくも同じ構造（あるステータスが高いほど成功しやすい／成功で全ステータス上昇／失敗で少し戻る）なので、**`showChallenge()` に統合**した。
GameEvent に以下を追加し、**JSONだけで新しい「ちょうせん」を作れる**ようにしてある。
| フィールド | 意味 | 既定値 |
|---|---|---|
| `challengeStat` | 判定に使うステータス(1〜3) | exam=1 / love=3 |
| `baseRate` | 基礎成功率(%) | exam=30 / love=20 |
| `passGain` | 成功時に全ステータスへ加算 | 20 |
| `failLoss` / `failMove` | 失敗時の減少・戻るマス | 3 / -2 |
| `passMessage` / `failMessage` | 結果の文言 | 汎用文 |

成功率 = `baseRate + 対象ステータス`（20〜95%でクランプ）。`EventKind.LOVE` を追加し、`hasLover` が立つと以後は発生しない。

### ちゅうがっこうステージ（23イベント）
- 背景14枚＋アイテム4枚（制服・体操服・ラケット・卒業証書）を追加。アイテムは小学校編と同じく淡いカードに中央配置
- 盤面背景は `bg_bukatsu`（夕焼けのグラウンド）。小学校の `bg_ground` と見分けがつく
- 💗こくはく（16マス目・ピンク）: **モテモテが高いほど成功しやすい**。成功で全ステータス+15
- 🌸高校受験（28マス目・赤）: 勉強しだい
- 条件ボーナス3種: 運動40以上でレギュラー / 運動35以上で体育祭さらにモテモテ+10 / モテモテ40以上でプレゼントさらに+10

### 通しバランス（3000試行のシミュレーション）
| ちょうせん | 平均成功率 | 中央 |
|---|---|---|
| 小学校の受験 | 47% | 45% |
| こくはく | 51% | 49% |
| 高校受験 | 72% | 73% |

ステージが進むほどステータスが積み上がるので**高校受験のほうが受かりやすい**。小学校で失敗しても取り返せる形になっている。最終ステータス合計は平均136。

### 今後（高校・大学）
`school_stages.json` の `stages` に足すだけ。**モテモテを活かす仕組みは `challengeStat: 3` の ちょうせん を置くのが基本形**で、高校では「こうはい ができる」「ぶんかさいの ステージ」など、大学では就職活動を `challengeStat: 1` で置くと筋が通る。

## v5.0: モード制と「しょうがっこう版」

### モード制（GameMode）
タイトルの「はじめる」→ **モード選択画面** → キャラ選択、という流れに変更。
```kotlin
enum class GameMode(val title, val desc, val file) {
    NORMAL("つうじょう版", "…", "stages.json"),
    SCHOOL("しょうがっこう版", "…", "school_stages.json")
}
```
`loadGameData()` が `mode.file` を読むだけでステージ一式が入れ替わる。**モード追加はJSONを1つ足して enum に1行書くだけ**。

### ステータス名の可変化（設計の要）
Player のフィールド（`manpuku` / `juujitsu` / `yuujou`）は**そのまま**で、**表示名だけ JSON の `statNames` で差し替える**方式にした。
| モード | stat1 | stat2 | stat3 |
|---|---|---|---|
| つうじょう | 満腹 | 充実 | 友情 |
| しょうがっこう | 勉強 | 運動 | モテモテ |

フィールド名を変える大改修を避けられ、スキル・スコア・図鑑など既存機能が全部そのまま動く。UIの表記（ステータスバー・ステータス画面・スキルボタン・最終結果）はすべて `statNames[n]` を参照するよう置換済み。
**注意**: `manpuku` という変数名が学校モードでは「勉強」を指す。読むときは「stat1」と考えること。

### 受験イベント（EventKind.EXAM）
- 最後のマス（28）に配置。`object Exam` に定数集約
- **合格率 = 30 + 勉強（20〜95%でクランプ）**。勉強を集めたかで変わる
- 合格 → 全ステータス +20、`passedExam = true`
- 不合格 → 勉強 -3、2マス戻る（再挑戦できる）
- シミュレーション（4000試行）: 到達時の合格率は 平均46% / 最小30% / 最大95%

### 条件ボーナス（運動が高いとモテモテが増える）
GameEvent に `condStat`(1〜3) `condMin` `condBonus1..3` `condMessage` を追加。
```json
{ "cell": 2, "bg": "bg_getabako", "message": "くつばこに てがみが！\nモテモテ+12",
  "yuujou": 12, "condStat": 2, "condMin": 25, "condBonus3": 8,
  "condMessage": "運動が たかいので さらに モテモテ+8！" }
```
条件成立時はイベント枠が**金色**になり、`condMessage` が追記される。

### しょうがっこう版の中身
- 1ステージ「しょうがっこう」30マス、イベント21件（🟢14 🟣6 🌸1）
- 背景9枚（通学路・教室・図書室・理科室・家庭科室・屋上・保健室・下駄箱・職員室）＋既存の校庭/体育館/プールを流用
- **アイテム5点**（黄色帽子・リコーダー・ランドセル・フラスコ・アルコールランプ）は白背景だったので、**淡いグラデーションのカードに中央配置して**イベント画像に仕立てた（680x380、他のイベント画像と同じ比率）
- `branchCell = -1` で**洞窟分岐なし**。`-1` を「分岐なし」として扱えるよう GameData と validate.py を対応
- 結婚・出産・しごと・おみせのマスも置いていない（`shared` は使う kind のぶんだけあればよい仕様に変更）

### 今後の拡張
中学校・高校・大学は `school_stages.json` の `stages` 配列に足すだけで、ステージ進行として繋がる。**モテモテは現状スコアに効くのみ**で、「中学校以降で役立つ」仕組み（恋愛イベントの条件など）は未実装。

## v4.6: badイベント追加とマスの色分け

### マスの色（指定どおり）
| 種類 | 色 | 記号 | 判定 |
|---|---|---|---|
| good | 緑 `#66BB6A` | ⭐ | 下記以外の通常イベント |
| **bad** | 紫 `#9575CD` | 💧 | **ステータス合計がマイナス、または もどる** |
| ワープ | オレンジ `#FF9800` | あな | `branchCell`（洞窟への分岐） |
| けっこん・出産 | ピンク `#F48FB1` | 💒 / 👶 | kind = wedding / birth |
| しごと | 水色 `#4DD0E1` | 💼 | kind = job |
| おみせ | 黄 `#FFD54F` | 🛒 | kind = shop |

- good/bad は **JSONに種別を持たせず、効果から自動判定**する（`MainActivity.isBad()`）。エディタで数値をマイナスにすれば自動で紫になるので、データ側で色を指定し忘れる事故が起きない
- `Board.badCells` / `Board.caveBadCells` を `applyStage()` で算出。ミニマップにも同じ配色が反映される
- エディタのマス一覧も同じ配色に統一（`EditorScreens` 側でも同じ式で bad 判定）

### badイベント19種（暗い写真14枚を使用）
| ステージ | 追加マス |
|---|---|
| もりのまち | 1(夜の森) 20(霧で迷う-2) 21(眠れない) 24(冷たいごはん) 27(あやしいかばん-3) 28(夜道) |
| とかいのステージ | 1(終電のがす) 5(寝過ごす-3) 14(予約なし) 21(交番) 23(のぼせる) 27(テスト) 28(ひとりの夜) |
| すなはまのステージ | 3(かぎ紛失-2) 11(はぐれる) 17(電車来ない-2) 23(食べそこね) 26(霧で進めない-4) 28(ころんだ) |

各ステージ 通常20件のうち good 11〜12 / bad 8〜9。ステージ合計は +157〜+192 でプラスを保っており、badを増やしてもステータスは伸びる。

### validate.py の追加検査
- **bad が good より多くないか**（警告）
- **通常イベントの合計がプラスか**（エラー。マイナスだとステータスが伸びずゲームが成立しない）
- 集計表示を `🟢12 🟣8 💒2 👶2 💼1 🛒2 … 合計+183` の形式に変更

## v4.5: 盤面のズーム切り替え

**きっかけ**: マスが大きすぎて先の展開が見えにくい、という指摘。従来は `spacing = 幅 / 3` の固定だった。

### 仕組み
- **`object Zoom`** に段階を保持。`steps = [3, 5, 8]`（画面に見えるマス数）。**BoardView はステージ切替のたびに作り直されるので、設定を BoardView 内に置くとリセットされてしまう**。Speed と同じくクラス外の object に持たせている
- `BoardView.applyMetrics()` にサイズ計算を切り出し、`onSizeChanged` と `cycleZoom` の両方から呼ぶ
- 凡例の行に **🔍ボタン**（紫）を追加。押すたび 3→5→8→3 と循環し、最大時は「（さいだい）」と表示
- **切り替え時は今の手番プレイヤーの位置を中心に保つ**（`cycleZoom(focusCell)` が camX を再計算）。倍率が変わっても見ている場所が飛ばない

### 縮小時の見やすさ対策
| 表示 | マス直径 | 文字 |
|---|---|---|
| 3マス | 194px | 20dp |
| 5マス | 130px | 13dp |
| 8マス | 89px | **11dp**（下限） |

- マスが潰れないよう、縮小するほど `cellRatio` を上げる（0.27 → 0.30 → 0.33）。マス同士が近づいて道が詰まって見える
- **文字サイズに 11dp の下限**を設けた。単純な比例だと8マス表示で9dp相当になり、💒👶⭐の判別が難しくなるため。`maxOf(cellR * 0.62f, 11f * density)`

（駒・目印・影はすべて `cellR` 基準なので自動で追従する）

## v4.4: キャラクター追加（ネズミ・ツル・カエル）— 全8体

| キャラ | 本人 | お嫁さん | 赤ちゃん |
|---|---|---|---|
| ネズミ | 3351 | 5007 | 5006（眠るネズミ） |
| ツル | 3357 | 5008 | 5009（卵から出るヒナ） |
| カエル | 3360 | 5004 | 5005の右側（オタマジャクシ） |

### 画像処理でつまずいた点（次回のために）
1. **ファイルと役割の取り違え**: 最初に9枚を機械的に割り当てたら、鶴・ネズミ・カエルの本人／嫁／子が総入れ替わりになった。**必ず一度サムネイルシートに並べて目視確認する**こと。3体×3枚を1枚のPNGにまとめて view すれば一目で分かる
2. **1枚に2体入っている画像**（5005 = 卵入りカエル＋オタマジャクシ）: 列ごとの不透明画素数から塊の切れ目を検出し、右側だけを crop してから透過処理した
3. **小さな塊の除去で目まで消えた**（ツルの赤ちゃん）: 「本体の5%未満の連結成分を消す」だけだと、白い顔に囲まれた**黒目も独立した小さな成分**なので一緒に消える。**面積が小さい かつ 平均明るさ195超**（＝卵の殻と同じ淡色）を条件にして解決。目や口は明るさ110程度なので残る

### 8体化に伴う修正
- キャラ選択グリッドの**列数を可変に**: 6体超なら3列、それ以下は2列（`cols = if (options.size > 6) 3 else 2`）。セル幅は `画面幅 / cols - gap` で算出
- 合計は最大4匹のままなので毎回4体が余る。CPUプール・イベントの `group=4` はいずれも余裕あり

**キャラ追加の手順**: ①3枚（本人/嫁/子）を切り抜いて drawable へ ②`charas` に1行追加 ③サムネイルで目視確認 ④`tools/validate.py`。列数はコードが自動対応する。

## v4.3: キャラクター追加（ペンギン）

**キャラは1体につき3枚セット**（本人 / お嫁さん / 赤ちゃん）。追加時はこの3枚が揃っていないと、結婚・出産イベントでパートナーや子どもが表示されず不利になる。

| 役割 | ファイル | 元画像 |
|---|---|---|
| 本人 | `chara_penguin.png` | 3296.png（正面） |
| お嫁さん | `chara_penguin_f.png` | 5002.png（エプロン姿） |
| 赤ちゃん | `child_penguin.png` | 5001.png（ヒナ） |

### 画像処理の注意（赤ちゃんペンギン）
元画像に**卵のカラがヒナと接して**描かれており、通常のフラッドフィル透過＋最大連結成分の抽出では分離できなかった。
対処: 行ごとの不透明範囲を調べてヒナ本体の x 範囲（287〜772）を特定し、**カラがはみ出す領域を切り落としてから**透過処理する、という順序にした。
```
crop((300, 130, 768, 852)) → フラッドフィル透過 → 最大連結成分のみ残す
```
同種の「本体に接した装飾物」が今後も来たら、この順序（先にトリミング→後で透過）を使うこと。

### 5体化に伴う修正
- キャラ選択グリッドを **2列固定・行数は匹数から算出**（`rowCount = (n + 1) / 2`）に変更。従来は `rowCount = 2; columnCount = 2` のハードコードで5体目が入らなかった
- 5体以上のときはセルサイズを縮小（`shrink` を 36dp → 56dp）して1画面に収まりやすくした
- 合計は最大4匹のままなので、キャラが1体余る構成になる。CPU用プール・イベントの `group=4`（friends 3体必要）はいずれも余裕あり

### 未使用の素材
4966（泣き顔）/ 4967（後ろ姿）/ 4968（横向き）は現在の設計に置き場所がないため未同梱。将来「マイナスイベントで表情が変わる」「盤面で進行方向を向く」を実装するなら活きる。

## v4.2.1: プレイヤー選択画面の整理

**問題**: 人数選択が「合計人数 × 人間の人数」の**7通りを1画面に縦並び**にしていて、何を選んでいるのか分かりにくかった（「3ひき 1P＋2P＋CPU1」のような密度の高いラベルが7行）。

**変更**: 独立した2つの決定を分離し、**3ステップ**にした。
1. **キャラ選択**（1P）
2. **あそびかた** — 「ひとりで」「ふたりで」の2択だけ。ふたりを選ぶと2Pキャラ選択が挟まる
3. **にんずう** — 合計何匹か（1人プレイなら1〜4、2人プレイなら2〜4）

**分かりやすくするための工夫**
- **並びを絵で見せる**: にんずう選択のカードに、実際に出てくるキャラ画像を 1P / 2P / CPU のラベル付きで横並び表示。誰が出るのか読まずに分かる
- **ステップ表示**: 各画面上部に `● キャラ ○ あそびかた ○ にんずう` を出して現在地を示す
- **もどるボタン**: 全画面に追加（以前は選び直せなかった）
- **2Pのプレースホルダ**: 「ふたりで あそぶ」のカードでは2人目を「？」アイコンで表示し、あとで選ぶことを示す

**共通パーツ**（`baseColumn` / `stepBar` / `titleText` / `subText` / `backButtonView` / `charaIcon` / `modeCard` / `lineupCard`）を用意して3画面のデザインを統一。`showCharaGrid` は step・sub・onBack を受け取れるよう拡張し、1P選択と2P選択で共用している。

**削除**: `showCountSelect()` と7通りの `combos` リスト。

## v4.2: 職業・資産・スキル（ロードマップ完走）

### assets/jobs.json（新規）
```json
{ "schemaVersion": 1, "startMoney": 200,
  "skills": [ { "id": "taiiku", "name": "たいりょく", "icon": "💪",
                "cost": 110, "dice": 1, "desc": "…" } ],
  "jobs":   [ { "id": "cook", "name": "コックさん", "icon": "🍳",
                "salary": 170, "requires": ["ryori"],
                "manpuku": 5, "juujitsu": 2, "yuujou": 0, "desc": "…" } ] }
```
- スキル5件 / 職業8件。`requires` が空の職業（むしょく・はなやさん）が必ずあるので、スキル0でも就職できる
- `dice` はスキルの常時ルーレット補正（たいりょく=+1）。0〜3にclamp
- **存在しないスキルを要求する職業は読み込み時に requires から除外**（永久に就けない職業を作らないため）

### Player の拡張
`money` / `jobId` / `skills: MutableSet<String>` を追加。`EventKind` に **JOB / SHOP** を追加。

### 新しいマス
| 種類 | 色 | 記号 | 配置 |
|---|---|---|---|
| 💼しごと | ティール | 💼 | 各ステージ1個（10 / 11 / 8） |
| 🛒おみせ | サーモン | 🛒 | 各ステージ2個（16,25 / 17,25 / 14,20） |
- `shared.job` / `shared.shop` に共通のメッセージ・背景を定義（結婚/出産と同じ継承方式）
- **CPUの行動**: 就職＝条件を満たすなかで最も給料の高い職業。買い物＝買えるなかで**最も安いものを1つだけ**（買い占めて有利になりすぎないよう抑制）

### 給料
`stageClear()` で**全員**が給料を受け取り、職業のステータス補正も加算される。次ステージ案内に内訳を表示。

### スコア設計（重要）
```
スコア = ステータス合計 + おかね/10 + きゅうりょう/10 + スキル数×10 + 家族(💍30 👶50)
```
**当初 `おかね/10` だけにしていたら「何も買わずに貯め込む」方が高得点になっていた**ため、職業とスキルへの投資も点になるよう変更した。Pythonでプレイパターンを再現して検証済み:

| プレイ | 最終職業 | 所持金 | スキル | スコア |
|---|---|---|---|---|
| 積極（全マス止まる） | スポーツせんしゅ | 280 | 4 | **259** |
| 半分止まる | コックさん | 320 | 3 | 245 |
| 就職のみ・貯金 | はなやさん | 500 | 0 | 225 |
| 何もしない | むしょく | 350 | 0 | 190 |

所持金だけなら貯金プレイが最多（500）だが、スコアでは積極プレイが上回る。**バランス調整は jobs.json の数値と `Skill.SKILL_SCORE` だけで完結する。**

### 経済バランスの初期調整
最初 startMoney 100 / スキル120〜220 / 給料30〜160 にしたところ、3ステージでスキル1個しか買えず職業が育たなかった。**startMoney 200 / スキル80〜150 / 給料50〜340** に変更して、積極プレイなら4スキル取得できるようにした。

### validate.py の追加検査
jobs.json: id重複 / **requires が実在するスキルか** / **スキル不要の職業が最低1つあるか**（ないと就職マスが機能しない）/ 最安スキルが初期資金+給料1回で買えるか / dice が0〜3。stages.json: 未知kindの検出、💼🛒の存在警告。

## v4.1: イベントエディタ＋図鑑

### 新規ファイル
- **`Zukan.kt`**（45行）— 遭遇済みイベントの記録。SharedPreferences に `"ステージ名:マス番号"` の Set で保存（DBは規約により不使用）。ステージ名を含めるのは別ステージの同じマス番号を区別するため。**`getStringSet` の返り値は直接編集してはいけない**ので `HashSet(cur)` でコピーしてから追加している
- **`EditorScreens.kt`**（469行）— エディタと図鑑のUI。MainActivityの肥大化を避けて分離。Activityは増やさず `act.setContentView` の差し替えで画面遷移する（既存方式と同じ）

### GameData に追加したAPI
| API | 用途 |
|---|---|
| `rawJson(ctx, file)` | 生JSON取得（filesDir優先） |
| `rawAssetJson(ctx, file)` | assets の初期データ |
| `saveJson(ctx, file, content)` | filesDir へ保存 |
| `resetToAssets(ctx, file)` | filesDir を削除して初期化 |
| `hasUserData(ctx, file)` | 編集済みかどうか |

**`saveJson` の安全策**: ①保存前に `JSONObject(content)` で構文チェック（壊れたJSONを書くと次回起動で読めなくなる）②`.tmp` に書いてから rename する（書き込み中の電源断で破損しない）

### 画面
- タイトルに **📖ずかん** と **✏️エディタ** のボタンを追加
- **図鑑**: 全イベント（本線53＋洞窟8）を一覧。遭遇済みは写真つき、未遭遇は「？？？」でグレー表示。「N / 61 こ みつけた！」の達成度つき。**記録は人間プレイヤーの遭遇のみ**（CPUでは埋まらない）
- **エディタ**: ステージ選択 → マス一覧 → 編集フォーム（メッセージ／満腹・充実・友情／すすむ・もどる／登場匹数）。背景写真をプレビュー表示
  - **💒結婚・👶出産のマスは編集不可**（`shared` を共有しているため個別編集するとゲーム設計が壊れる）。タップすると案内が出る
  - **「はじめの ないように もどす」** で filesDir を削除して assets の初期状態へ復帰。編集済みのときだけ有効
  - 入力値は保存時に clamp（ステータス ±999 / 移動 ±10 / 匹数 1〜4）。数値入力は `TYPE_NUMBER_FLAG_SIGNED` を付けてマイナスを入れられるようにしている

### 編集の反映
`onDataChanged = { loadGameData() }` で保存直後にデータを読み直す。ゲーム中ではなくタイトル配下の画面なので、進行中の状態を壊さない。

### 検証
エディタの読み書きロジックをPythonで再現し、**編集の往復で他ステージ・shared・💒👶の定義・イベント件数がいずれも無傷**であることを確認済み。編集後のJSONが `tools/validate.py` を通ることも確認。validate.py は新規2ファイルの構文チェックにも対応させた。

## v4.0b: データ駆動化 完了

### assets/stages.json（本線3ステージ・53イベント）
```json
{ "schemaVersion": 1, "mainCellCount": 30,
  "shared": { "wedding": {...}, "birth": {...} },
  "stages": [
    { "name": "もりのまち", "bg": "bg_forest", "branchCell": 5,
      "events": [
        { "cell": 2, "bg": "bg_famiresu", "kind": "normal",
          "message": "…", "manpuku": 15, "juujitsu": 0,
          "yuujou": 5, "group": 3, "move": 0 },
        { "cell": 7, "kind": "wedding" }
      ] } ] }
```
- **`shared` による継承**: 結婚/出産イベントは全ステージ共通なので `shared.wedding` / `shared.birth` に本体を1回だけ書き、各ステージでは `{"cell": 7, "kind": "wedding"}` とマス番号だけ書く。個別に項目を書けばそちらが優先される（`GameData.parseEvent()` の `str()`/`num()` が個別→shared→デフォルトの順に解決）
- `Board.MAIN_COUNT` も `const` から **var** にし、`mainCellCount` で上書き
- 洞窟の盤面背景も `cave.json` の `bg` から取るようにし、Kotlin側の `R.drawable.bg_*` 直接参照は**ゼロ**になった（残るのはキャラ・目印のみ）

### 移行の進め方（手写しをしない）
Kotlinのステージ定義は**正規表現で機械的に抽出**してJSON化した。抽出件数と元の `N to ` 行数を突き合わせ、3ステージとも 18/17/18 で一致することを確認してから旧定義を削除している。手で書き写すと必ず取りこぼすので、今後の大量移行も同じ方法を取ること。

### tools/validate.py（同梱）
JSONを手編集したら **push前に `python3 tools/validate.py`** を実行する。Kotlinコンパイラのない環境でも、データ起因の不具合はここでほぼ潰せる。検査内容:
- stages: schemaVersion / マス重複 / 範囲1〜28 / branchCellの非重複と1〜9 / 💒👶が2つずつ / **最初の💒より後に👶があるか（出産が発生不能でないか）** / 移動なしが多数派 / message非空 / group1〜4 / 画像の実在
- cave: マス重複・範囲・画像 / 全ステージの復帰先が盤内 / **洞窟内でスタート(0)に戻る経路がないか全開始位置×出目1〜10で総当たり**
- Kotlin: 括弧の対応 / **`"$変数日本語"` の検出**（v3.3でビルドを壊した罠）
- エラーがあれば終了コード1。スクリプト自体も、わざと壊したJSONで検出できることを確認済み

### コード量
MainActivity.kt 1590行 → **1449行**（データがJSONへ移った分）／ GameData.kt 296行 ／ stages.json 14KB

## v4.0a.1 バグ修正: 洞窟でスタートに戻る

**症状**: 洞窟内でイベントに止まったあと、次のターンで洞窟のスタート(0)に戻っている。

**原因（主因）**: ✨充実スキル（自分以外を2マス戻す）が **世界をまたいで作用していた**。
`for (other in players) if (other !== p)` で全員を対象にしていたため、本線にいるCPUがスキルを使うたびに洞窟内のプレイヤーも2マス戻され、洞窟の浅い位置（1〜2）にいると0＝スタートまで押し戻される。CPUは毎ターン40%で発動するので、イベントのダイアログを閉じた直後に頻発していた。

**修正**
1. `pushOthers(p)` / `canPushOthers(p)` を新設し、**`other.inCave == p.inCave` の相手だけ**を対象にする。人間・CPU・ボタンの有効判定すべてを同じ関数に集約。メッセージも「みんなを」→「◯ひきを」に変更
2. `movePiece` に `backLimit` を導入。**洞窟では後退の下限を 1 にする**（0は入口＝スタートなので「振り出しに戻った」ように見えるため）。本線は従来どおり0
3. `enterCave` の冒頭に `if (p.inCave) return` の二重入場ガード
4. `BoardView.snapToCell(i)` を新設。**世界が切り替わるとき（本線↔洞窟）はカメラをアニメせず即座に合わせる**。従来は本線座標から洞窟座標へアニメしていたため、盤面を横断する不自然な動きになっていた

**検証**: 洞窟の全開始位置×全出目（1〜10、スキル補正込み）を総当たりし、0に到達する経路が0件であることを確認済み。

## v4.0a の主要変更（データ駆動化・前半）

**設計書（人生ゲームアプリ_スマホ版設計書.md）に沿った v4系ロードマップ**
1. **v4.0 データ駆動化** ← イマココ（a=洞窟 / b=本線3ステージ）
2. v4.1 イベントエディタ＋図鑑
3. v4.2 職業・資産

### 新規ファイル `GameData.kt`
JSON読み込み基盤。**外部依存ゼロを維持するため Android標準の `org.json` のみ使用**。
- `loadBoard(context, fileName, defaultName, defaultCellCount): LoadResult`
- `LoadResult(data: BoardData, warnings: List<String>)` — **例外を投げず、壊れた項目はスキップして警告を積む**。データが壊れてもアプリは起動する
- 読み込み優先順: **`filesDir/{file}.json` → `assets/{file}.json`**（v4.1のエディタが filesDir に書き出す前提）
- 画像は文字列名で持ち `resources.getIdentifier()` で解決。見つからなければ `bg_forest` にフォールバック。結果は `resCache` に保持
- 警告がある場合は Toast で先頭3件を表示（開発中に気づけるように）

### `assets/cave.json`（schemaVersion 1）
```json
{ "schemaVersion": 1, "name": "どうくつ", "cellCount": 20,
  "returnSkip": 20, "bg": "bg_cave",
  "events": [ { "cell": 3, "bg": "bg_cave", "kind": "normal",
                "message": "…", "manpuku": 0, "juujitsu": 15,
                "yuujou": 0, "group": 1, "move": 0 } ] }
```
- `kind`: `normal` / `wedding` / `birth`（未知の値は normal 扱い）
- `cell` は 1〜cellCount-2（S と G には置けない）
- `Board.CAVE_COUNT` は **const から var に変更**し、JSONの `cellCount` で上書きされる
- `CAVE_SKIP` も JSONの `returnSkip` で駆動

### 変更していないこと（意図的）
**リグレッションゼロが v4.0a の成功条件**。本線3ステージは Kotlin 定義のまま、ゲーム内容・バランス・UIは一切変更していない。JSON化の不具合と新機能の不具合を切り分けられるようにするため。

### 検証スクリプト（納品前に実行）
構文（括弧対応）/ 文字列テンプレート結合の危険箇所ゼロ / JSON: schemaVersion・マス重複・範囲・画像実在・kind既知・移動なしが半数以上・returnSkip妥当 / drawable参照の実在

## v3.3の主要変更

### 人間2人プレイ（1台のスマホでパス&プレイ）
- `Player.humanNo`（1P/2P、CPUは0）を追加
- キャラ選択を `showCharaGrid(title, options, onPick)` に共通化。1P選択 → 人数選択 → （人間2人なら）残り3匹から2P選択 → `buildPlayers()`
- 人数選択は **(合計人数 × 人間の人数)** の7通りをボタンで提示（人間2人の選択肢は青緑 `#00897B`、1人は黄緑 `#7CB342`）
- 表示名は `who(p)` に集約: CPU=「名前（CPU）」/ 人間が1人=「名前（きみ）」/ 人間が2人=「名前（1P）」「名前（2P）」
- ステータスバーは **手番プレイヤー** の値を表示（従来は人間1人固定）。人間2人のときは先頭に「1P 」「2P 」を付ける
- スキル・スタートボタン・盤面スライドはいずれも `players[turn].isHuman` 判定なので2Pでもそのまま機能する

### ルーレットの挙動（「ふつう」のみ）
- カスタム `TimeInterpolator snapSpin` を追加。**前半72%の時間で回転量の88.5%を等速の高速回転**、残り28%で一気に減速して止まる
  - 係数は接続点で速度が連続するよう調整済み（t=0.71で1.229 → t=0.73で1.146、末端で0）
  - 従来の `DecelerateInterpolator(2.2)` は t=0.5 時点で95%回り終えていた → 新カーブは61.5%で、直前まで速く回る
- 「はやい」は元々1.1秒と短いため従来どおり `DecelerateInterpolator(2.2f)`

### 画像
- **背景はすべてJPEG**（43枚）。透過が必要なキャラクター8体・赤ちゃん4体・目印3種・アイコンのみPNG（16枚）
- 背景を元画像から680px幅・quality80・プログレッシブで作り直し（再圧縮の劣化を避けるため既存JPEGではなく uploads の原本から再生成）
- drawable合計 4,491KB → **3,338KB**（約26%削減）

## v3.2の主要変更

### ⚠️ 移動マスの廃止（設計変更）
- **盤面から「+n / -n」の移動マスを完全に撤去**。`Board.moves` / `Board.caveMoves` / `movesOf()` / `Stage.moves` / fwdPaint / backPaint はすべて削除済み
- 代わりに `GameEvent.dMove: Int` を追加し、**イベントの結果として** すすむ/もどるが発生する
  - `showEvent()` の OK ハンドラでステータス加算後、`dMove != 0` なら `movePiece(p, dMove, fromEvent = true)`
  - `onLanded()` からマス由来の移動処理は消滅し、イベント判定→`nextTurn()` のみ
- **移動なしのイベントを多数派にする方針**。現状 各ステージ 移動あり4件 / 移動なし13〜14件、洞窟 移動あり4件 / なし4件
- マスの種類は S / G / ⭐イベント / 💒結婚 / 👶出産 / 「あな」分岐 / 通常（番号）のみ

### ステージ別の盤面背景
- `Stage.bgRes` を追加し、`BoardView(context, players, mainBgRes)` に渡す（洞窟背景は常に `bg_cave`）
| ステージ | 名前 | 背景 |
|---|---|---|
| 1 | もりのまち | `bg_forest`（既存・生成） |
| 2 | とかいのステージ | `bg_city`（PIL生成: ビル群・窓の灯り・道路） |
| 3 | すなはまのステージ | `bg_sand`（PIL生成: 空・海・白波・砂・貝がら） |

### マスの目印（ランドマーク）
- `mark_cave.png`（洞窟の入口）/ `mark_chapel.png`（チャペル）/ `mark_heart.png`（ハート）をPILで生成
- `drawTrack()` の冒頭で、分岐マス・結婚マス・出産マスの**後ろに**（マスの円と駒より先に）描画。サイズ `cellR*2.3`、底辺 `laneY - cellR*0.35`
- 出産マスの目印は `lockedBirth()` が false のとき（＝誰かが結婚済み）だけ表示され、マスの出現と連動する

### 追加イベント（写真12枚）
- ステージ1: 広い公園 `bg_park2` / お花見 `bg_hanami`(+2) / 桜の川 `bg_sakura_river` / 森の家 `bg_woodhouse`
- ステージ2: 町の交差点 `bg_town` / 都会の駅 `bg_citystation` / いなかの駅 `bg_countrystation`(-2) / 学校 `bg_schoolyard`
- ステージ3: ボウリング `bg_bowling` / 大聖堂 `bg_cathedral` / 雨の学校 `bg_rainschool`(-2) / 新しいモール `bg_mall2`
- 洞窟にもイベント追加（8マス）: 3ひかる石 / 5みちにまよう(-3) / 7こうもり(-2) / 9ちかどう(+2) / 11ちかすい / 13ひろば / 15たからばこ / 17すべった(-3)

### 検証スクリプト（納品前に実行）
イベントマスの重複なし / 分岐マスが1〜9かつイベントと非重複 / 💒👶が2つずつ / 範囲1〜28 / **移動なしイベントが多数派** / 洞窟が19未満 / drawable参照の実在・未使用ゼロ

## v3.1の主要変更（洞窟ルート分岐）
- **うさぎのキャラ画像を差し替え**（`chara_usagi.png`。お嫁さん・赤ちゃんは据え置き）
- **洞窟背景 `bg_cave.jpg` をPILで生成**（900x700。岩肌グラデーション・天井の鍾乳石・床の石筍・奥の光・ガウスぼかしで発光させた結晶22個）

### 2ワールド構造
- `Player.inCave` / `Player.caveReturn` を追加。`position` は「今いる世界の中での位置」を表す
- `Board` が2ワールド対応に: `count(cave)` / `goal(cave)` / `movesOf(cave)`（本線30マス・洞窟20マス）
  - 旧 `Board.CELL_COUNT` `Board.GOAL_INDEX` は**廃止**。全参照を置き換え済み
- `BoardView.world`（false=本線 / true=洞窟）を `startTurn()` で手番プレイヤーに合わせて切替。背景・マス数・マス色・駒すべてが切り替わる
  - `visiblePlayers()` が同じ世界にいるプレイヤーだけを返す（駒とミニマップ両方で使用）
  - 背景は `world` に応じて森／洞窟を選び、既存のミラータイル＋パララックスをそのまま使う

### 分岐の流れ
1. 本線の**分岐マス（紫・「あな」表示）**に止まる → `enterCave()`
2. `caveReturn = 分岐マス + CAVE_SKIP(20)` を記録し、`inCave = true` / `position = 0` で洞窟へ
3. 洞窟20マスを進み、ゴール(19)に到達 → `exitCave()`
4. `inCave = false` / `position = caveReturn` に戻し、**`onLanded()` を通常どおり呼ぶ**ので復帰先のイベントや移動マスも発動する
   - 復帰先(20〜29)は分岐マス(1〜9)と絶対に重ならないため無限ループの心配なし
- ステージ移行時・リスタート時は `inCave` / `caveReturn` もリセット

### 分岐マスと洞窟の配置
| ステージ | 分岐マス | 復帰先 |
|---|---|---|
| もりのまち | 5 | 25（通常マス） |
| たびのステージ | 7 | 27（-3マス） |
| あそびのステージ | 1 | 21（+2マス） |

洞窟（全ステージ共通・20マス）: イベント 3(ひかる石 充実+15) / 7(こうもり 充実-10) / 11(ちかすい 満腹+10 充実+5) / 15(たからばこ 3種+15)、移動 2:-2, 5:-3, 9:+2, 13:-3, 17:-3

- **検証スクリプト**: 分岐マスが他と重複しないこと、1〜9の範囲（+20が盤内）に収まること、洞窟の配置が19未満であることを納品前にチェック

## v3.0の主要変更（ステージ制）

### ステージ進行
- 誰か1人がゴールすると **全員** つぎのステージへ。位置だけ0に戻し、ステータス・結婚・子どもは引き継ぐ
- ゴールした本人に1着ボーナス（充実+10 友情+10、`Skill.CLEAR_BONUS`）。次ステージは**その勝者の手番から**開始
- 最終ステージのゴールで `showFinalResult()`：満腹+充実+友情の合計でランキング表示（🥇🥈🥉、💍/👶マーク付き）

### データ構造の変更（重要）
- `Stage(name, events: Map<Int, GameEvent>, moves: IntArray)` を導入し、`stages: List<Stage>` で全ステージを定義
- `Board` の盤面データは **var** になり、`applyStage()` がステージ切替時に差し替える
  - `Board.moves` / `Board.normalEventCells` / `Board.weddingCells` / `Board.birthCells`
- `GameEvent.kind: EventKind {NORMAL, WEDDING, BIRTH}` に変更（旧 `isWedding`/`requiresMarriage`/`isBirth` フラグを統合）
- 発生条件は `eventAvailable()` に集約: NORMAL=常に / WEDDING=未婚のみ / BIRTH=結婚済みかつ子どもなし
- **結婚マス💒と出産マス👶は各ステージ2箇所ずつ**。達成済みなら止まっても何も起きない（素通り）
- 出産マスは結婚済みプレイヤーが1人もいない間は通常マス表示（`lockedBirth()`）。マス色: 結婚=ピンク`#F8BBD0` / 出産=アンバー`#FFE082`

### ステージ構成（すべて30マス）
| | ステージ1「もりのまち」 | ステージ2「たびのステージ」 | ステージ3「あそびのステージ」 |
|---|---|---|---|
| イベント | ファミレス2/海4/公園6/野菜9/プール11/図書館13/喫茶店15/校庭18/映画22/ログハウス26 | バス2/電車4/空港6/パリ9/客船12/温泉15/旅館18/登山22/スキー26 | モール2/遊園地4/観覧車7/ウォーターパーク10/体育館13/入院16(マイナス)/映画19/レストラン22/喫茶店25/公園27 |
| 💒結婚 | 7, 19 | 8, 20 | 6, 18 |
| 👶出産 | 14, 23 | 13, 24 | 12, 24 |
| もどる | 8マス | 10マス | 10マス |
| すすむ | 2マス | 2マス | 3マス |

- 背景画像は760px幅JPEG（quality82）で追加15枚。drawable合計 約3.3MB
- **マス配置の検証は自動化推奨**: イベントマスと移動マスの重複、結婚/出産が2箇所ずつ、もどる>すすむ、を正規表現でチェックするスクリプトを納品前に実行している

## v2.5の主要変更（出産・家族システム）
- **赤ちゃんキャラ4体**: `child_shiba / child_usagi / child_inoshishi / child_tora`。`Chara(name, resId, partnerRes, childRes)` に追加
  - いのししの素材のみ背景・「1歳」ラベル・地面付きだったため、トリミング→フラッドフィル透過→「幅が広すぎる下端行の除去」→最大連結成分のみ残す、の順で地面と草を除去して生成
- **出産イベント（21マス目）**: `bg_hospital` ＋ `requiresMarriage = true, isBirth = true`。満腹+15 充実+30 友情+30。OKで `Player.hasChild = true`
- **結婚しないと出現しないマス**: `Board.lockedEventCells`（`events` の `requiresMarriage` から自動生成）
  - 盤面・ミニマップとも、結婚済みプレイヤーが誰もいない間は**通常マス（番号表示）**として描画。誰かが結婚するとピンク色の 👶 マスに変化（`locked(i)` 判定）
  - 未婚のプレイヤーが止まっても何も起きない。出産は1人1回まで（`isBirth && hasChild` で抑止）
- **家族が増えるほど強くなる**: `gain()` の倍率が 独身1.0倍 → 結婚1.5倍 → 子どもあり**2.0倍**（プラスのみ、マイナスは等倍）
- **一緒に行動**: 盤面では 本人→パートナー→子ども の順に並んで歩く（子どもは本人の0.52倍）。イベント写真にも家族全員が登場（子どもはパートナーの0.72倍）
- ステータスバー/ステータス画面は 👶x2.0 が 💍x1.5 を上書き表示

## v2.4の主要変更（結婚システム）
- **お嫁さんキャラ4体**: `chara_shiba_f / usagi_f / inoshishi_f / tora_f`。`Chara(name, resId, partnerRes)` で本人と1対1に紐付け（結婚相手は同じ種類のキャラ）
- **結婚イベント（14マス目）**: `bg_chapel` ＋ `isWedding = true`。満腹+10 充実+20 友情+20。OKで `Player.married = true`
  - 結婚式自体の上昇は1.5倍の対象外（`married` を立てるのは加算処理の後）
- **結婚後の効果**
  - イベントでのプラスのステータス上昇が **1.5倍**（`gain()` ヘルパー、`Math.round`。マイナス値は等倍のまま）
  - 以後すべてのイベント写真にパートナーが並んで登場（本人のすぐ右、やや小さめ・-4°傾き）
  - 盤面の駒の隣にパートナーが並んで歩く（本人の0.78倍、影付き、本人の後ろ側に描画）
  - ステータスバーに `💍x1.5`、ステータス画面に「けっこん済み」を表示
- **画像の軽量化**: キャラ8体をPILの Fast Octree で200色パレット化（1体400KB→約55KB）。透過とフラット塗りの見た目は維持。drawable合計 3.0MB→1.8MB

## v2.3の主要変更
- **ランチャーアイコン**: PILで生成した6色ルーレット＋赤い針のデザイン（432px、`ic_launcher.png`）
- **森の背景**: PILで生成した `bg_forest.jpg`（900x700、空・雲・遠景の山・針葉樹3層・草地）。`BoardView.drawForest()` がカメラの0.25倍でパララックス表示。1枚を左右反転しながら並べて継ぎ目なくループ（`Math.floorMod(i,2)` で反転判定）。道の色は森に映える土色 `#C9A66B` に変更
- **スキル3種**（定数は `object Skill` に集約）
  | スキル | コスト | 効果 | 実装 |
  |---|---|---|---|
  | 🍖 パワーアップ | 満腹 -10 | 次のルーレット +3 | `Player.boostNext` フラグ。`onRouletteResult` で加算し消費 |
  | ✨ おいかけ | 充実 -10 | 自分以外全員を2マス戻す | 即時発動。位置は0未満にならない。イベントは再発動しない |
  | 🤝 友情ボーナス | なし（パッシブ） | 友情30以上で常にルーレット +1 | `Skill.YUUJOU_THRESHOLD`。※仕様の「30を超える」は実装上「30以上」 |
- スキルボタンは手番テキストの下に2つ並ぶ。自分の番かつコスト充足時のみ有効（無効時は alpha 0.4）
- **CPUもスキルを使う**: 手番開始時に充実スキル40%・満腹スキル50%の確率で発動（`cpuUseSkills()`）
- ステータスバーに発動中のボーナス（🤝+1 / 🍖+3）を表示。ルーレット結果は「うさぎ は「4」🍖+3 🤝+1 → 8 マス！」のように内訳表示

## v2.2の主要変更（イベント10種）
| マス | イベント | 背景 | 満腹 | 充実 | 友情 | 匹数 |
|---|---|---|---|---|---|---|
| 2 | レストランなな（ファミレス） | bg_famiresu | +15 | - | +5 | 3 |
| 4 | うみ | bg_beach | +5 | - | +10 | 3 |
| 6 | こうえん | bg_park | - | +10 | - | 1 |
| 9 | 無人野菜販売所 | bg_yasai | +10 | +5 | - | 1 |
| 11 | プール | bg_pool | -5 | +10 | +5 | 3 |
| 13 | 図書館 | bg_library | - | +15 | - | 1 |
| 15 | 喫茶店なな | bg_cafe | +8 | +8 | - | 2 |
| 18 | グラウンド（校庭） | bg_ground | -5 | - | +15 | 4 |
| 22 | 映画館 | bg_cinema | - | +12 | +8 | 2 |
| 26 | ログハウス | bg_cabin | +10 | +10 | +10 | 4 |

- 背景画像は800px幅のJPEG（quality 85）に統一。drawable合計 約2.8MB
- `groupSize` 最大4に対応。`showEvent()` は横並びLinearLayoutで本人を中央寄りに配置（1匹:本人のみ / 2匹:本人+友 / 3匹:友-本人-友 / 4匹:友-本人-友-友）。4匹時はサイズを縮小（本人92dp・友70dp）
- マイナス値イベント対応のためステータスは `coerceIn(0, 999)` でクランプ
- **イベントマスと CELL_MOVE マスは重複禁止**（現在: イベント 2,4,6,9,11,13,15,18,22,26 / 移動 8,10,12,16,20,24,27）

## v2.1の主要変更
- **マス縮小**: `spacing = 幅/3`（画面に約3マス）、`cellR = spacing*0.27`、レーンは盤面ビュー下部（laneY=h*0.72）
- **ミニマップ**: 盤面ビュー上部（h*0.04〜0.34）に白枠パネル。全30マスを種別色の小円で表示＋全キャラの現在位置を小さいビットマップで表示（手番キャラは大きめ）。`cellFill(i)` をトラックと共用
- **スライド確認**: 自分の番のみ `panEnabled=true`。`onTouchEvent` で camX をドラッグ（0〜GOALにクランプ）。スタート押下→`focusCell(自位置, fast)` で戻り、380ms後にルーレット始動
- **イベント汎用化**: `GameEvent(bgRes, message, dManpuku, dJuujitsu, dYuujou, groupSize)` と `events: Map<Int, GameEvent>`。`Board.eventCells` は onCreate で events のキーから設定。groupSize>1 なら本人＋他キャラを傾き付きで並べて楽しそうに合成（左-10°/中央+4°/右+10°）
- **イベント一覧**: 4=うみ（bg_beach, 満腹+5 友情+10, 3匹）/ 6=こうえん（bg_park, 充実+10, 1匹）
- **CELL_MOVE変更**: 4マス目の+2は10マス目へ移動（イベントと重複回避）。現在: 8:-3, 10:+2, 12:+3, 16:-2, 20:+2, 24:-4, 27:-3

## v2.0の主要変更
- **キャラ画像を透過切り抜き**: 外周につながる白/薄グレーのみ透過（キャラ内部の白は保持）。処理は納品前にPILで実施済み、リポジトリ内は透過PNG
- **盤面を直線レーン化**: 30マスを左→右一直線に配置。`BoardView` はカメラ方式（`camX`=画面中央のワールドX）で横スクロール。`focusCell(i, slow)` で手番プレイヤーを常に中央へ（slow=1300ms: 後方プレイヤーへゆっくり戻る演出 / fast=220ms: 移動追従）。可視範囲のマスのみ描画
- **レイアウト**: 上半分=盤面 / 凡例+スピード行 / 手番テキスト / ルーレット(左,weight1.3)+ボタン列(右: スタート=オレンジ#FF9800・ステータス=緑#4CAF50) / 最下部に細い統計バー
- **ステータス3種**: `Player.manpuku/juujitsu/yuujou`（満腹・充実・友情、0〜999、`min(999, +n)`でキャップ）。バーは人間プレイヤーの値、ステータスボタンで全員分ダイアログ表示
- **公園イベント（6マス目）**: `Board.CELL_EVENT[6]=true`（紫⭐マス）。停止すると bg_park.jpg＋止まったキャラ画像(左下100dp)＋メッセージ枠「今回は\n公園で楽しく遊んだ。充実が10」→OKで充実+10。CPUが止まっても発動（統計バーは人間のみ反映）
- ボタン背景は `GradientDrawable` をコードで生成（roundedBg()）。XMLドローアブル不使用の規約は維持

## v1.2で実装済みの仕組み
- `Speed` object: ふつう/はやい の2段階。回転時間・移動間隔・待ち時間・回転数を一括切替（ゲーム画面右上のボタン）
- `Board.CELL_MOVE: IntArray(30)`: +nで「nマスすすむ」(青マス)、-nで「nマスもどる」(赤マス)。配置: 4:+2, 8:-3, 12:+3, 16:-2, 20:+2, 24:-4, 27:-3
- イベント移動は**連鎖なし**（`movePiece(fromEvent=true)` → `onLanded(fromEvent=true)` でイベント再発動をスキップ）。ループ発生の心配なし
- 駒の立体表現: 足元に楕円影（浮くと影が縮む）＋手番キャラはsin波でふわふわジャンプ（BoardView内の無限ValueAnimator、attach/detachで開始停止）

## フェーズ2の残タスク（ここから着手する）
1. 1回休み: `Player.skipNext: Boolean` を `startTurn()` 冒頭でチェック
2. 選択イベント（「およぐ？ 休む？」のような分岐ダイアログ）
3. スキル・結婚倍率のバランス調整（`object Skill` の定数のみで変更可能）
4. 発展案: 子どもの成長イベント、洞窟ルートを各ステージ固有にする、分岐を「行く/行かない」の選択制にする、ステージごとに本線の背景を変える（現在は全ステージ bg_forest）
3. ゴール時にステータス合計で結果発表（先着＋ステータスの複合スコア案）
4. 検討事項: ゴールぴったり停止ルール、満腹の自然減少（ターン経過で-1等）
5. イベント追加は `events` マップに `位置 to GameEvent(...)` を1行足すだけ（背景jpgを drawable に追加）

## フェーズ3の残タスク
- 複数の人間プレイヤー（同一端末パス&プレイ）: 人数選択画面で「にんげん/CPU」の割当UIを追加し、`Player.isHuman` を切り替えるだけで手番ロジックはそのまま使える
